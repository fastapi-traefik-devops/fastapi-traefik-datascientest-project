# Placeholder values for local development only.
# Real deployments must override these via a secret generator, sealed-secrets,
# an external secrets operator, or another out-of-band mechanism.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
type: Opaque
stringData:
  SECRET_KEY: changethis
  FIRST_SUPERUSER_PASSWORD: changethis
  SMTP_PASSWORD: ""
  POSTGRES_PASSWORD: changethis
